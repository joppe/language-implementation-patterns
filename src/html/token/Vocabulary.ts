export enum Vocabulary {
    BSLASH = '\\',
    DBQUOTES = '"',
    EQUALS = '=',
    FSLASH = '/',
    GT = '>',
    LT = '<',
    MINUS = '-',
    UNDERSCORE = '_',
}

export const WHITESPACE_RE: RegExp = /\s/;
